# La carpeta app/ NO se versiona: la genera el flujo de publicacion copiando la app ya publicada
# (herramientas/copiar-app.mjs). Asi no hay dos copias del mismo codigo que puedan desincronizarse.
app/
